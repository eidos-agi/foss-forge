.build/
build/
.swiftpm/
DerivedData/
# Commit Package.resolved for apps and executables (reproducible builds);
# ignore it only in libraries, where consumers pick their own versions.
*.xcuserstate
xcuserdata/
.DS_Store
